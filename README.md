# ArduPilot WASM SITL Demo

Runs ArduPlane SITL in the browser with WebAssembly and communicates with it over MAVLink.

## Run

```sh
sudo apt install npm
npm install
npm run dev
```

The development server supplies the required cross-origin isolation headers.